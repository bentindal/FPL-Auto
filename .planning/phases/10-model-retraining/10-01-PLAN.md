---
phase: 10-model-retraining
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [fpl_auto/retrainer.py, tests/test_retrainer.py, data/2024-25/accumulated_gw.csv]
autonomous: true
requirements: [MR-01]
must_haves:
  truths:
    - "LiveDataCollector fetches FPL API bootstrap-static + element-summary endpoints"
    - "Data merged and validated: >500 players, >400 actuals per GW"
    - "accumulated_gw.csv exists with schema [gw, player_id, position, team, xp, minutes, goals, assists, xg, xa, shots, key_passes, bps, points]"
  artifacts:
    - path: fpl_auto/retrainer.py
      provides: "LiveDataCollector + FPLDataSource classes"
    - path: tests/test_retrainer.py
      provides: "Test suite with 5+ test cases"
    - path: data/2024-25/accumulated_gw.csv
      provides: "Accumulated gameweek data schema"
  key_links:
    - from: "LiveDataCollector"
      to: "FPL Official API"
      via: "requests HTTP calls"
---

<objective>
Implement FPL + Understat data collection pipeline and populate accumulated_gw.csv for the 2024-25 season.

Purpose: Enable retraining pipeline to access live player performance data.

Output: LiveDataCollector class tested against APIs; accumulated_gw.csv initialized with correct schema.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-model-retraining/10-CONTEXT.md
@.planning/research/10-MODEL-RETRAINING-RESEARCH.md
@CLAUDE.md

From 10-CONTEXT.md:
- Data sources: FPL Official API + Understat (understatapi library)
- Schema: [gw, player_id, position, team, xp, minutes, goals, assists, xg, xa, shots, key_passes, bps, points]
- QA validation: >500 players, >400 actuals per GW, positions in [GK, DEF, MID, FWD]

From fpl_auto/predictor.py:
- POSITIONS = ['GK', 'DEF', 'MID', 'FWD']
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FPLDataSource wrapper for official FPL API</name>
  <files>fpl_auto/retrainer.py</files>
  <action>
Create new file fpl_auto/retrainer.py with FPLDataSource class wrapping official FPL API.

Implementation:
- Class FPLDataSource with __init__(base_url='https://fantasy.premierleague.com/api/', session=None)
- Methods: fetch_bootstrap_static(), fetch_element_summary(player_id), fetch_fixtures(), fetch_gw_live(gw)
- Error handling: requests.ConnectionError → return None + log; HTTP 429 → exponential backoff (max 3 retries); timeout → log warning
- Store requests.Session() on instance (pattern from fpl_auto/data.py)
  </action>
  <verify>
    <automated>grep -q "class FPLDataSource" fpl_auto/retrainer.py && grep -q "def fetch_bootstrap_static" fpl_auto/retrainer.py</automated>
  </verify>
  <done>FPLDataSource class implemented with 4 fetch methods and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Create LiveDataCollector with Understat merge</name>
  <files>fpl_auto/retrainer.py</files>
  <action>
Extend fpl_auto/retrainer.py with LiveDataCollector class.

Implementation:
- Class LiveDataCollector with __init__(fpl_source: FPLDataSource)
- Method collect_week(gw: int) → pd.DataFrame:
  - Fetch FPL bootstrap-static + element-summary
  - Build FPL dataframe: [player_id, position, team, minutes, goals, assists, xp, bps]
  - Fetch Understat via understatapi.UnderStat().get_player_matches(season=2024)
  - Merge on player_id (FPL primary, Understat left-join)
  - Add [xg, xa, shots, key_passes] from Understat (NaN if missing)
  - Add gw column
  - Return sorted by player_id
- Error handling: Understat unavailable → fall back to FPL-only (log warning)
  </action>
  <verify>
    <automated>grep -q "class LiveDataCollector" fpl_auto/retrainer.py && grep -q "def collect_week" fpl_auto/retrainer.py && grep -q "understatapi" fpl_auto/retrainer.py</automated>
  </verify>
  <done>LiveDataCollector with FPL + Understat merge implemented; Understat fallback working</done>
</task>

<task type="auto">
  <name>Task 3: Add validation and accumulated_gw.csv handling</name>
  <files>fpl_auto/retrainer.py</files>
  <action>
Add validation function and CSV append logic.

Implementation:
- Function validate_week_data(gw_data: pd.DataFrame, gw: int) → bool:
  - Assert len(gw_data) > 500 (raise ValueError if not)
  - Assert gw_data['points'].notna().sum() > 400
  - Assert gw_data['xp'].notna().sum() > 400
  - Assert gw_data['position'].isin(['GK', 'DEF', 'MID', 'FWD']).all()
  - Log "GW{gw} validated: {len(gw_data)} records"
- Function append_to_accumulated_csv(gw_data: pd.DataFrame, season: str, gw: int):
  - file_path = Path(f'data/{season}/accumulated_gw.csv')
  - Create file with schema if doesn't exist
  - Validate gw_data, add gw column, reorder columns
  - Append to CSV via pd.concat()
  - Log "Appended GW{gw}: {len(gw_data)} rows"

Schema order: gw, player_id, position, team, xp, minutes, goals, assists, xg, xa, shots, key_passes, bps, points
  </action>
  <verify>
    <automated>grep -q "def validate_week_data" fpl_auto/retrainer.py && grep -q "def append_to_accumulated_csv" fpl_auto/retrainer.py && grep -q "assert len(gw_data) > 500" fpl_auto/retrainer.py</automated>
  </verify>
  <done>Validation and CSV append functions implemented with schema enforcement</done>
</task>

<task type="auto">
  <name>Task 4: Test LiveDataCollector with mocks</name>
  <files>tests/test_retrainer.py</files>
  <action>
Create tests/test_retrainer.py with test suite using unittest.mock.

Test class TestLiveDataCollector with 5 test cases:

1. test_fpl_api_fetch: Mock FPL API, verify 600+ players returned
2. test_understat_merge: Mock Understat, verify merge with NaN handling for missing players
3. test_data_validation_pass: Valid data (550+ players, 450+ actuals, valid positions) → passes
4. test_data_validation_fail: Invalid data (<500 players) → raises ValueError
5. test_accumulated_csv_write: Write 50 rows GW1 + 60 rows GW2, verify CSV schema + row count

Use unittest.mock.patch() for API mocks. Use tempfile.TemporaryDirectory() for CSV test.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_retrainer.py::TestLiveDataCollector -v 2>&1 | grep -E "(PASSED|passed)"</automated>
  </verify>
  <done>Test suite with 5 test cases created; all tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External APIs → LiveDataCollector | Untrusted: malformed JSON, missing fields, type mismatches |
| File system → accumulated_gw.csv | CSV may be corrupted from prior runs |

## STRIDE Threats

| ID | Category | Component | Mitigation |
|----|----------|-----------|-----------|
| T-10-01 | Tampering | accumulated_gw.csv | Validate row count + schema on append |
| T-10-02 | Spoofing | FPL API endpoint | HTTPS + official domain (trust FPL) |
| T-10-03 | Repudiation | API failures | Log all calls (timestamp, status) + FPL-only fallback |
| T-10-04 | Information Disclosure | Player data | Public data (no PII) |
| T-10-05 | Denial of Service | Rate limiting | Exponential backoff, max 3 retries, 60s cooldown |
</threat_model>

<verification>
1. LiveDataCollector instantiates with FPLDataSource, requests.Session stored on instance
2. FPL API calls return >500 players
3. Understat merge completes with xG/xA columns (NaN if missing)
4. accumulated_gw.csv schema: 14 columns in correct order
5. Test suite passes; validation catches <500 players and invalid positions
</verification>

<success_criteria>
- LiveDataCollector.collect_week(gw) fetches FPL + Understat, merges on player_id
- accumulated_gw.csv exists with correct schema
- 5+ test cases pass; validation catches invalid data
- Rate limits → backoff + retry; Understat unavailable → FPL-only + log warning
- MR-01: Data collection pipeline ready for daily post-GW execution
</success_criteria>

<output>
After completion, create `.planning/phases/10-model-retraining/10-01-SUMMARY.md`
</output>
