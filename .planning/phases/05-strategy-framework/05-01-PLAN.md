---
phase: 05-strategy-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [fpl_auto/strategies.py, manager.py]
autonomous: true
requirements: [SF-01, SF-02, SF-03]
user_setup: []

must_haves:
  truths:
    - "StrategyConfig dataclass with ~15 parameters is defined and importable"
    - "Three strategy archetypes (conservative, aggressive, differential-focused) are instantiable"
    - "manager.py accepts --strategy parameter and passes it to run_season()"
    - "Each strategy's parameter choices are interpretable and traceable"
  artifacts:
    - path: "fpl_auto/strategies.py"
      provides: "StrategyConfig + 5 preset configs (static, baseline, conservative, aggressive, differential)"
      exports: "StrategyConfig, BASELINE_STATIC, BASELINE_CURRENT, CONSERVATIVE, AGGRESSIVE, DIFFERENTIAL"
    - path: "manager.py"
      provides: "CLI --strategy parameter + strategy instantiation"
      contains: "parse_args update, run_season accepts strategy config"
  key_links:
    - from: "manager.py (main)"
      to: "run_season(config)"
      via: "strategy config passed in config dict"
      pattern: "config\\['strategy'\\]"
    - from: "run_season"
      to: "team.auto_transfer/auto_captain/auto_chips"
      via: "strategy.transfer_mode, strategy.captain_mode, strategy.chip_schedule"
      pattern: "strategy\\.(transfer|captain|chip)_mode"
---

<objective>
Define parametrizable strategy system and create three representative archetypes.

Purpose: Establish a flexible, testable strategy framework that Phase 5-8 will evaluate and compare systematically. Strategies are encoded as StrategyConfig objects, enabling clean comparison without ad-hoc code branching.

Output: (1) StrategyConfig dataclass with ~15 parameters covering transfer, captain, chip, bench, and risk decisions; (2) Five preset configs (two baselines + three archetypes); (3) manager.py --strategy parameter accepting strategy name and instantiating the config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/STRATEGY_EVALUATION.md
@.planning/phases/04-feature-engineering/04-03-SUMMARY.md

**Key Reference from STRATEGY_EVALUATION.md:**
- Section 3 defines StrategyConfig structure with ~15 interpretable parameters
- Section 1 recommends baselines: Static (never transfer) + Current (existing manager.py)
- Transfer modes: 'never', 'flexible', 'greedy'
- Captain modes: 'highest_xp', 'highest_value', 'form_based'
- Chip schedules: 'never', 'conservative', 'aggressive'
- Bench modes: 'static', 'rotate_low_xp', 'fixture_aware'

**Current manager.py strategy logic (implicit):**
- Transfer: auto_transfer() with flexible logic (existing implementation)
- Captain: auto_captain() highest xP selected
- Chips: auto_chips() adaptive scheduling
- Bench: auto_subs() based on next GW xP
This becomes BASELINE_CURRENT archetype.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design and Implement StrategyConfig Dataclass</name>
  <files>fpl_auto/strategies.py</files>
  <action>
Create new file fpl_auto/strategies.py with StrategyConfig dataclass (per STRATEGY_EVALUATION.md Section 3).

Include these ~15 parameters (all fields must have sensible defaults):
- **Transfer policy:** transfer_mode ('never', 'flexible', 'greedy'), max_transfers_per_gw (0-2), transfer_discount_factor (0.6-1.0)
- **Captaincy policy:** captain_mode ('highest_xp', 'highest_value', 'form_based'), captain_lookback_gws (1-5), captain_variance_penalty (0.0-0.5)
- **Chip usage policy:** chip_schedule ('never', 'conservative', 'aggressive'), wildcard_threshold_points (30-80), chip_budget_limit (0-3)
- **Bench policy:** bench_mode ('static', 'rotate_low_xp', 'fixture_aware'), bench_injury_threshold (0.0-1.0)
- **Risk parameters:** position_variance_tolerance (0.8-1.5), punt_threshold (0.0-2.0)

All parameters must be interpretable (e.g., 'conservative' not 0.74). Use sensible type hints (str for modes, int for counts, float for factors).

Include a __post_init__ method that validates:
- transfer_mode in ['never', 'flexible', 'greedy']
- captain_mode in ['highest_xp', 'highest_value', 'form_based']
- chip_schedule in ['never', 'conservative', 'aggressive']
- bench_mode in ['static', 'rotate_low_xp', 'fixture_aware']
- All float ranges are within bounds (e.g., transfer_discount_factor in [0.6, 1.0])
- Raise ValueError if validation fails

Add brief docstring explaining each parameter's effect on strategy behavior.
  </action>
  <verify>
    <automated>python3 -c "from fpl_auto.strategies import StrategyConfig; c = StrategyConfig(); print(f'Config: {c.transfer_mode}, {c.captain_mode}, {c.chip_schedule}'); assert c.transfer_mode in ['never', 'flexible', 'greedy']"</automated>
  </verify>
  <done>fpl_auto/strategies.py exists with StrategyConfig dataclass, all 15 parameters defined with defaults, validation implemented, no import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Five Preset Strategy Configs (Baselines + Archetypes)</name>
  <files>fpl_auto/strategies.py</files>
  <action>
In fpl_auto/strategies.py, add five preset StrategyConfig instances:

1. **BASELINE_STATIC** (per STRATEGY_EVALUATION.md Option A):
   - transfer_mode='never' (no transfers ever)
   - captain_mode='highest_value' (captain highest-priced player)
   - chip_schedule='never'
   - bench_mode='static'
   - position_variance_tolerance=1.0, punt_threshold=1.0 (conservative)

2. **BASELINE_CURRENT** (per STRATEGY_EVALUATION.md Option B — current manager.py logic):
   - transfer_mode='flexible' (current auto_transfer behavior)
   - captain_mode='highest_xp'
   - captain_lookback_gws=1
   - chip_schedule='conservative'
   - bench_mode='rotate_low_xp'
   - transfer_discount_factor=0.8 (matches current _get_n_gws_xp discount factor)
   - max_transfers_per_gw=1
   - position_variance_tolerance=1.2
   - punt_threshold=0.5

3. **CONSERVATIVE** (risk-averse, stable returns):
   - transfer_mode='flexible'
   - max_transfers_per_gw=0 (rare transfers only when forced)
   - captain_mode='highest_xp'
   - captain_variance_penalty=0.3 (penalize high-variance players)
   - chip_schedule='conservative'
   - wildcard_threshold_points=60
   - bench_mode='static'
   - position_variance_tolerance=0.9
   - punt_threshold=1.0

4. **AGGRESSIVE** (maximize upside, accept volatility):
   - transfer_mode='greedy'
   - max_transfers_per_gw=2
   - transfer_discount_factor=0.6 (longer lookahead discount)
   - captain_mode='highest_xp'
   - captain_lookback_gws=3
   - captain_variance_penalty=0.0 (embrace variance)
   - chip_schedule='aggressive'
   - wildcard_threshold_points=40
   - chip_budget_limit=3 (use all chips)
   - bench_mode='fixture_aware'
   - position_variance_tolerance=1.5
   - punt_threshold=0.1

5. **DIFFERENTIAL** (beat-the-crowd strategy, high upside/downside):
   - transfer_mode='flexible'
   - max_transfers_per_gw=1
   - captain_mode='form_based' (opposite of crowd)
   - captain_lookback_gws=5
   - captain_variance_penalty=-0.2 (prefer contrarian picks)
   - chip_schedule='aggressive'
   - wildcard_threshold_points=50
   - bench_mode='fixture_aware'
   - position_variance_tolerance=1.4
   - punt_threshold=0.2

Each config must be instantiable (no errors when created). Add a comment explaining each archetype's philosophy (e.g., "CONSERVATIVE: prioritizes consistency over upside").

Export all five in __all__ = ['StrategyConfig', 'BASELINE_STATIC', 'BASELINE_CURRENT', 'CONSERVATIVE', 'AGGRESSIVE', 'DIFFERENTIAL']
  </action>
  <verify>
    <automated>python3 -c "from fpl_auto.strategies import BASELINE_STATIC, BASELINE_CURRENT, CONSERVATIVE, AGGRESSIVE, DIFFERENTIAL; print('All 5 configs instantiated'); assert BASELINE_STATIC.transfer_mode == 'never'; assert BASELINE_CURRENT.transfer_mode == 'flexible'; assert AGGRESSIVE.max_transfers_per_gw == 2"</automated>
  </verify>
  <done>All five configs defined, instantiable without errors, docstrings explain philosophy, exported in __all__.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor manager.py to Accept --strategy Parameter</name>
  <files>manager.py</files>
  <action>
Update manager.py to accept a --strategy parameter:

1. **Update parse_args():**
   - Add new argument: `parser.add_argument('-strategy', type=str, default='baseline_current', choices=['static', 'baseline_current', 'conservative', 'aggressive', 'differential'], help='Strategy to use for season simulation')`

2. **Create strategy instantiation function:**
   ```python
   def get_strategy_config(strategy_name: str):
       from fpl_auto.strategies import (
           BASELINE_STATIC, BASELINE_CURRENT, CONSERVATIVE, AGGRESSIVE, DIFFERENTIAL
       )
       strategies = {
           'static': BASELINE_STATIC,
           'baseline_current': BASELINE_CURRENT,
           'conservative': CONSERVATIVE,
           'aggressive': AGGRESSIVE,
           'differential': DIFFERENTIAL,
       }
       if strategy_name not in strategies:
           raise ValueError(f"Unknown strategy: {strategy_name}")
       return strategies[strategy_name]
   ```

3. **Modify run_season(config) signature:**
   - run_season already accepts config dict
   - Add 'strategy' key to config dict before passing to run_season
   - Inside run_season, the strategy is available as config['strategy']

4. **Update main():**
   ```python
   def main():
       inputs = parse_args()
       strategy_config = get_strategy_config(inputs.strategy)
       
       # Prepare config for run_season
       if inputs.seasons:
           configs = [
               {
                   'season': season,
                   'start_gw': inputs.start_gw,
                   'repeat': inputs.repeat_until - inputs.start_gw,
                   'starting_team': inputs.starting_team,
                   'quiet': True,
                   'strategy': strategy_config,
               }
               for season in inputs.seasons
           ]
           # ... existing multiprocessing.Pool logic ...
       else:
           config = {
               'season': inputs.season,
               'start_gw': inputs.start_gw,
               'repeat': inputs.repeat_until - inputs.start_gw,
               'starting_team': inputs.starting_team,
               'quiet': False,
               'strategy': strategy_config,
           }
           result = run_season(config)
   ```

5. **No changes needed to run_season() signature** — it already accepts config dict and can extract strategy via config['strategy']

**Important:** Do NOT modify team.py or team methods yet. This task only wires the strategy parameter through the CLI and config dict. Actual strategy usage (team.auto_transfer checks strategy.transfer_mode, etc.) will be implemented in Plan 05-02.
  </action>
  <verify>
    <automated>python3 manager.py -h | grep -i strategy</automated>
  </verify>
  <done>manager.py --strategy parameter added, choices=['static', 'baseline_current', 'conservative', 'aggressive', 'differential'], default='baseline_current', strategy instantiated and passed in config dict to run_season().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → config dict | User supplies strategy name; must validate before instantiation |
| config dict → run_season | Strategy config must be valid StrategyConfig object |
| StrategyConfig → team methods | Strategy parameters used by team.auto_transfer, etc. (validated in __post_init__) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | StrategyConfig parsing | mitigate | Validate all parameters in __post_init__; raise ValueError if invalid |
| T-05-02 | Information Disclosure | Strategy parameters | accept | No secrets in StrategyConfig; all parameters are public game logic |
| T-05-03 | Denial of Service | Parameter grid search | accept | Phase 6-8 will validate parameter ranges; Phase 5 uses hardcoded presets |

</threat_model>

<verification>
- [ ] fpl_auto/strategies.py exists with StrategyConfig and 5 presets
- [ ] StrategyConfig has all 15 parameters with defaults
- [ ] StrategyConfig.__post_init__ validates parameter ranges
- [ ] All 5 preset configs instantiate without errors
- [ ] manager.py accepts -strategy parameter
- [ ] manager.py maps strategy name to StrategyConfig instance
- [ ] strategy is passed in config dict to run_season()
- [ ] No modifications to team.py (wiring only, not implementation)
- [ ] CLI help text documents strategy choices
</verification>

<success_criteria>
1. StrategyConfig dataclass created with ~15 parameters (transfer, captain, chip, bench, risk)
2. All parameters have sensible defaults and type hints
3. Validation in __post_init__ enforces valid choices for enum-like fields
4. Five preset configs defined: BASELINE_STATIC, BASELINE_CURRENT, CONSERVATIVE, AGGRESSIVE, DIFFERENTIAL
5. manager.py --strategy parameter accepts strategy name, instantiates config, passes to run_season()
6. No errors when running: `python3 manager.py -season 2021-22 -strategy conservative`
7. Strategy parameter correctly wired through config dict (not yet used by team methods — that's Plan 05-02)
</success_criteria>

<output>
After completion, create `.planning/phases/05-strategy-framework/05-01-SUMMARY.md`
</output>
