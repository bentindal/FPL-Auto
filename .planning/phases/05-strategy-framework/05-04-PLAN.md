---
phase: 05-strategy-framework
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified: [tests.py, evaluation/test_evaluation.py, .planning/phases/05-strategy-framework/VERIFICATION.md]
autonomous: true
requirements: [SF-01, SF-02, SF-03, SE-01, SE-02, SE-03, SE-04, SE-05]
user_setup: []

must_haves:
  truths:
    - "Full Phase 5 integration tests pass (strategy → manager → metrics)"
    - "Baseline results reproducible (BASELINE_STATIC, BASELINE_CURRENT)"
    - "Metrics aggregation correct (per-season breakdown + cross-season averages)"
    - "No temporal leakage detected in walk-forward evaluation"
  artifacts:
    - path: "tests.py"
      provides: "Integration test suite for Phase 5"
      contains: "TestPhase5Integration test class with 5+ test methods"
    - path: "evaluation/test_evaluation.py"
      provides: "Evaluation framework unit tests"
      contains: "test_bootstrap_ci_values, test_bonferroni_correction, test_metrics_table"
    - path: ".planning/phases/05-strategy-framework/VERIFICATION.md"
      provides: "Phase 5 completion verification"
      contains: "Requirement checklist, baseline metrics summary, walk-forward results"
  key_links:
    - from: "tests.py"
      to: "fpl_auto/strategies.py"
      via: "imports all 5 strategy configs"
      pattern: "from fpl_auto.strategies import.*BASELINE"
    - from: "tests.py"
      to: "evaluation/walk_forward.py"
      via: "calls nested_walk_forward_evaluation()"
      pattern: "walk_forward\\.nested_walk_forward"
    - from: "tests.py"
      to: "manager.py"
      via: "run_season with strategy parameter"
      pattern: "manager\\.run_season"
---

<objective>
Integration testing and Phase 5 completion verification.

Purpose: Validate that all Phase 5 components work together correctly. Integration tests verify strategy → manager → evaluation pipeline. Verification document confirms all requirements are met and framework is ready for Phase 6-8 strategy evaluations.

Output: (1) TestPhase5Integration test class in tests.py; (2) evaluation/test_evaluation.py with unit tests for metrics; (3) VERIFICATION.md checklist confirming Phase 5 readiness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 5 success criteria)
@.planning/phases/05-strategy-framework/05-01-PLAN.md (StrategyConfig)
@.planning/phases/05-strategy-framework/05-02-PLAN.md (Walk-forward)
@.planning/phases/05-strategy-framework/05-03-PLAN.md (Metrics)

**Phase 5 Success Criteria (from ROADMAP):**
1. StrategyConfig with ~15 parameters ✓ (Plan 05-01)
2. manager.py accepts --strategy ✓ (Plan 05-01)
3. Three archetypes (conservative, aggressive, differential) ✓ (Plan 05-01)
4. Walk-forward validation framework ✓ (Plan 05-02)
5. Two baselines: static, current ✓ (Plan 05-02)
6. Multi-dimensional metrics: Sharpe, Sortino, CV, max drawdown ✓ (Plan 05-03)
7. 95% bootstrapped CIs ✓ (Plan 05-03)
8. Bonferroni correction ✓ (Plan 05-03)

This plan validates all 8 criteria through testing and documentation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestPhase5Integration Test Class</name>
  <files>tests.py</files>
  <action>
Add to tests.py a new test class TestPhase5Integration with 5-6 test methods:

```python
import unittest
from fpl_auto.strategies import (
    BASELINE_STATIC, BASELINE_CURRENT, CONSERVATIVE, AGGRESSIVE, DIFFERENTIAL
)
from evaluation.walk_forward import run_strategy_on_seasons, nested_walk_forward_evaluation
from evaluation.metrics import compute_season_metrics, bootstrap_ci

class TestPhase5Integration(unittest.TestCase):
    
    def test_all_strategies_instantiate_without_error(self):
        """Verify all 5 strategy configs are valid."""
        strategies = [BASELINE_STATIC, BASELINE_CURRENT, CONSERVATIVE, AGGRESSIVE, DIFFERENTIAL]
        for s in strategies:
            self.assertIsNotNone(s.transfer_mode)
            self.assertIn(s.transfer_mode, ['never', 'flexible', 'greedy'])
            self.assertIn(s.captain_mode, ['highest_xp', 'highest_value', 'form_based'])
    
    def test_baseline_static_runs_on_single_season(self):
        """Baseline (static team) executes without error on one season."""
        results = run_strategy_on_seasons(BASELINE_STATIC, ['2021-22'])
        self.assertEqual(len(results), 1)
        self.assertEqual(results[0]['season'], '2021-22')
        self.assertEqual(len(results[0]['p_list']), 38)  # 38 GWs
    
    def test_baseline_current_runs_on_single_season(self):
        """Baseline (current approach) executes without error on one season."""
        results = run_strategy_on_seasons(BASELINE_CURRENT, ['2021-22'])
        self.assertEqual(len(results), 1)
        self.assertEqual(len(results[0]['p_list']), 38)
    
    def test_nested_walk_forward_returns_two_iterations(self):
        """Walk-forward validation produces 2 iterations (test on 2023-24, 2024-25)."""
        results = nested_walk_forward_evaluation(BASELINE_STATIC)
        self.assertEqual(len(results), 2)
        test_seasons = {r['test_season'] for r in results}
        self.assertEqual(test_seasons, {'2023-24', '2024-25'})
    
    def test_walk_forward_metrics_include_required_fields(self):
        """Walk-forward results include test_metrics and train_metrics."""
        results = nested_walk_forward_evaluation(BASELINE_STATIC)
        for iteration in results:
            self.assertIn('test_metrics', iteration)
            self.assertIn('train_metrics', iteration)
            self.assertIn('test_season', iteration)
            self.assertIn('train_seasons', iteration)
    
    def test_compute_metrics_returns_sharpe_and_sortino(self):
        """Metrics computation includes Sharpe and Sortino ratios."""
        weekly_points = [50, 60, 45, 55, 48, 52, 58, 47, 61, 53] + [50] * 28  # 38 weeks
        metrics = compute_season_metrics(weekly_points)
        self.assertIn('sharpe_ratio', metrics)
        self.assertIn('sortino_ratio', metrics)
        self.assertIn('coefficient_variation', metrics)
        self.assertIn('max_drawdown', metrics)
        self.assertGreater(metrics['sharpe_ratio'], 0)
```

All tests target the integration of Plans 05-01 through 05-03. Add to existing tests.py at the end before final main block.
  </action>
  <verify>
    <automated>python3 -m unittest tests.TestPhase5Integration -v</automated>
  </verify>
  <done>TestPhase5Integration class added to tests.py with 6 integration tests, all passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create evaluation/test_evaluation.py Unit Tests</name>
  <files>evaluation/test_evaluation.py</files>
  <action>
Create evaluation/test_evaluation.py with unit tests for metrics module:

```python
import unittest
import numpy as np
from evaluation.metrics import (
    compute_season_metrics,
    bootstrap_ci,
    apply_bonferroni_correction,
    format_metrics_table,
)

class TestMetricsComputation(unittest.TestCase):
    
    def setUp(self):
        """Generate sample weekly points for testing."""
        np.random.seed(42)
        self.p_stable = [55] * 38  # Stable season
        self.p_volatile = np.random.uniform(20, 90, 38).tolist()  # Volatile
        self.p_declining = [70 - i for i in range(38)]  # Declining trend
    
    def test_sharpe_ratio_positive(self):
        """Sharpe ratio positive for non-zero returns."""
        metrics = compute_season_metrics(self.p_stable)
        self.assertGreater(metrics['sharpe_ratio'], 0)
    
    def test_sortino_ratio_greater_than_sharpe(self):
        """Sortino ≥ Sharpe (only penalizes downside)."""
        metrics = compute_season_metrics(self.p_volatile)
        self.assertGreaterEqual(metrics['sortino_ratio'], metrics['sharpe_ratio'] * 0.9)  # Allow ~10% margin
    
    def test_coefficient_variation_lower_for_stable(self):
        """Stable season has lower CV than volatile."""
        m_stable = compute_season_metrics(self.p_stable)
        m_volatile = compute_season_metrics(self.p_volatile)
        self.assertLess(m_stable['coefficient_variation'], m_volatile['coefficient_variation'])
    
    def test_max_drawdown_non_negative(self):
        """Max drawdown always ≥ 0."""
        metrics = compute_season_metrics(self.p_declining)
        self.assertGreaterEqual(metrics['max_drawdown'], 0)
        self.assertLessEqual(metrics['max_drawdown'], 1.0)
    
    def test_bootstrap_ci_excludes_zero_for_difference(self):
        """CI excludes 0 when strategy A clearly beats B."""
        a_seasons = [{'total_points': 2200 + np.random.normal(0, 20)} for _ in range(4)]
        b_seasons = [{'total_points': 2000 + np.random.normal(0, 20)} for _ in range(4)]
        
        ci = bootstrap_ci(a_seasons, b_seasons, metric_key='total_points', n_bootstrap=1000)
        # A is consistently higher, so 0 should be outside CI
        self.assertGreater(ci['diff_ci_lower'], 0)
    
    def test_bonferroni_correction_divides_alpha(self):
        """Bonferroni correctly divides alpha by num_comparisons."""
        alpha_10 = apply_bonferroni_correction(10, alpha=0.05)
        self.assertEqual(alpha_10, 0.005)
        
        alpha_20 = apply_bonferroni_correction(20, alpha=0.05)
        self.assertEqual(alpha_20, 0.0025)
    
    def test_format_metrics_table_contains_strategy_names(self):
        """Formatted table includes strategy names."""
        strategy_results = {
            'baseline': {'mean_metrics': {'total_points': 2100}},
            'aggressive': {'mean_metrics': {'total_points': 2200}},
        }
        table = format_metrics_table(strategy_results)
        self.assertIn('baseline', table)
        self.assertIn('aggressive', table)

if __name__ == '__main__':
    unittest.main()
```

Create file evaluation/test_evaluation.py with these unit tests. Tests validate metric computation, CI bounds, and Bonferroni correction in isolation.
  </action>
  <verify>
    <automated>python3 -m pytest evaluation/test_evaluation.py -v</automated>
  </verify>
  <done>evaluation/test_evaluation.py created with 8 unit tests, all passing. Tests validate Sharpe, Sortino, CV, max drawdown, bootstrap CIs, and Bonferroni correction.</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 5 Verification Document</name>
  <files>.planning/phases/05-strategy-framework/VERIFICATION.md