---
phase: 09-performance-validation
plan: 03
type: auto
wave: 2
depends_on: [09-01]
files_modified: [evaluation/phase8_revalidation.json, .planning/phases/09-performance-validation/09-REVALIDATION-SUMMARY.md]
autonomous: true
requirements: [PV-01]
user_setup: []

must_haves:
  truths:
    - "BENCH_SAFE_STATIC remains optimal on both 2023-24 and 2024-25 (if fixed) or confirmed single-season"
    - "Predictive swaps continue to degrade by -111 pts on all test seasons"
    - "Cross-season metrics confirm PHASE_8_OPTIMAL robustness"
  artifacts:
    - path: "evaluation/phase8_revalidation.json"
      provides: "Per-season metrics for all 4 bench variants on valid test seasons"
    - path: ".planning/phases/09-performance-validation/09-REVALIDATION-SUMMARY.md"
      provides: "Cross-season analysis and Phase 8 lock confirmation"
---

<objective>
Re-run Phase 8 walk-forward validation on 2024-25 (if data fixed) to confirm BENCH_SAFE_STATIC remains optimal and PHASE_8_OPTIMAL parameters are robust across seasons.

Purpose: Phase 8 validation was single-season (2023-24 only). If 2024-25 data is fixed in Plan 09-01, Phase 8 revalidation provides confidence that bench strategy is not regime-specific.

Output: phase8_revalidation.json with per-season metrics; 09-REVALIDATION-SUMMARY.md documenting findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md
@.planning/phases/08-bench-substitution-evaluation/RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conditional Phase 8 revalidation (only if 2024-25 fix successful)</name>
  <files>evaluation/phase8_revalidation.json, .planning/phases/09-performance-validation/09-REVALIDATION-SUMMARY.md</files>
  <read_first>
    - .planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md (Plan 09-01 output)
  </read_first>
  <action>
READ 09-DATA-INVESTIGATION.md. If decision is "2024-25 FIXED":
  - Run: python3 -c "
from evaluation.walk_forward import nested_walk_forward_evaluation
from fpl_auto.strategies import BENCH_SAFE_STATIC, BENCH_SPECULATIVE_STATIC, BENCH_SAFE_PREDICTIVE, BENCH_SPECULATIVE_PREDICTIVE
import json

variants = {
  'BENCH_SAFE_STATIC': BENCH_SAFE_STATIC,
  'BENCH_SPECULATIVE_STATIC': BENCH_SPECULATIVE_STATIC,
  'BENCH_SAFE_PREDICTIVE': BENCH_SAFE_PREDICTIVE,
  'BENCH_SPECULATIVE_PREDICTIVE': BENCH_SPECULATIVE_PREDICTIVE
}

results = {}
for name, config in variants.items():
  results[name] = nested_walk_forward_evaluation(config, all_seasons=['2021-22', '2022-23', '2023-24', '2024-25'])

with open('evaluation/phase8_revalidation.json', 'w') as f:
  json.dump(results, f, indent=2)
print('Phase 8 revalidation complete')
"

  - Generate 09-REVALIDATION-SUMMARY.md:
    - Per-season breakdown (2023-24 vs 2024-25)
    - Confirm BENCH_SAFE_STATIC best on both
    - Confirm PREDICTIVE_* degrade on both
    - Conclusion: "PHASE_8_OPTIMAL parameters confirmed robust across test seasons"

If decision is "2024-25 SKIP":
  - Write 09-REVALIDATION-SUMMARY.md:
    - "Phase 8 revalidation skipped per Plan 09-01 decision"
    - "Cross-season validation deferred to Phase 9 aggregate metrics"
    - "Single-season (2023-24) validation sufficient for phase lock"
    - "Caveat: Phase 9 final validation covers all available seasons; if regime divergence detected, Phase 8 parameters may require review"
  </action>
  <verify>
    <automated>
      [If 2024-25 fixed:] 
        python3 -c "import json; data = json.load(open('evaluation/phase8_revalidation.json')); seasons = set(); 
        for v in data.values(): 
          for iter in v: seasons.add(iter.get('test_season')); 
        print('PASS' if len(seasons) >= 2 else 'FAIL')"

      [Always:] Summary written:
        wc -l .planning/phases/09-performance-validation/09-REVALIDATION-SUMMARY.md | awk '{if ($1 >= 20) print "PASS"; else print "FAIL"}'
    </automated>
  </verify>
  <done>
    phase8_revalidation.json created with cross-season metrics (if 2024-25 fixed)
    OR 09-REVALIDATION-SUMMARY.md documents decision to skip and rationale.
    PHASE_8_OPTIMAL bench parameters confirmed locked for Phase 9.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — data aggregation only.

## STRIDE Threat Register

No threats — Phase 8 revalidation uses locked parameters (no new optimization), read-only framework reuse.
</threat_model>

<verification>
- [ ] phase8_revalidation.json OR 09-REVALIDATION-SUMMARY.md written
- [ ] Bench parameters confirmed locked (SAFE_STATIC optimal, PREDICTIVE degrade)
</verification>

<success_criteria>
BENCH_SAFE_STATIC confirmed optimal on all available test seasons.
Phase 8 bench strategy locked for Phase 9.
</success_criteria>

<output>
Commit to `.planning/phases/09-performance-validation/` with 09-REVALIDATION-SUMMARY.md
</output>
