---
phase: 09-performance-validation
plan: 01
type: auto
wave: 1
depends_on: []
files_modified: [predictions/2024-25/, data/2024-25/, model.py]
autonomous: true
requirements: [PV-01, PV-02]
user_setup: []

must_haves:
  truths:
    - "2024-25 predictions exist for all positions (GK, DEF, MID, FWD) at all gameweeks"
    - "Team initialization succeeds on 2024-25 GW1 without 'no GK available' error"
    - "Root cause of initialization failure documented"
  artifacts:
    - path: "predictions/2024-25/"
      provides: "GW1-GW38 predictions for all positions"
      min_lines: 20
    - path: ".planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md"
      provides: "Root cause analysis and fix decision"
  key_links:
    - from: "team.py __init__"
      to: "data/get_predictions(gw, pos)"
      via: "auto_subs() bench selection"
      pattern: "get_predictions.*GK"
---

<objective>
Investigate and fix 2024-25 season data initialization error blocking walk-forward validation.

Purpose: Phase 8 validation failed on 2024-25 with "Squad has no GK available for bench selection". Phase 9 requires cross-season validation to confirm PHASE_8_OPTIMAL robustness. Fix data or document caveat.

Output: Fixed predictions/2024-25/ structure OR documented decision to proceed with 2023-24-only validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-performance-validation/09-RESEARCH.md
@.planning/phases/09-performance-validation/09-CONTEXT.md
@.planning/phases/08-bench-substitution-evaluation/RESULTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate 2024-25 data structure and root cause</name>
  <files>.planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md</files>
  <read_first>
    - /Users/bentindal/Desktop/coding/FPL-Auto/fpl_auto/data.py (get_predictions method signature)
    - /Users/bentindal/Desktop/coding/FPL-Auto/fpl_auto/team.py (auto_subs() line where error occurs)
    - /Users/bentindal/Desktop/coding/FPL-Auto/predictions/2024-25/ (directory structure)
    - /Users/bentindal/Desktop/coding/FPL-Auto/data/2024-25/ (CSV availability)
  </read_first>
  <action>
Investigate root cause of "Squad has no GK available for bench selection" error from Phase 8 Plan 03.

Steps:
1. List all files in predictions/2024-25/GW1/ — verify GK.tsv exists with data
2. Compare predictions/2024-25/ structure to 2023-24 working structure
3. Check data/2024-25/ CSV files (players, fixtures) for completeness
4. Trace error in team.py auto_subs() → get_predictions() call path
5. Determine if issue is:
   - Missing GK.tsv file (regeneration needed via model.py -save)
   - Incomplete CSV data (missing players or fixtures)
   - Logic edge case (GK assumption in code)

Document findings in 09-DATA-INVESTIGATION.md with:
- Root cause diagnosis (specific file/logic)
- Fix recommendation (regenerate vs skip 2024-25)
- Effort estimate for fix (quick <30min vs complex >1hr)
  </action>
  <verify>
    <automated>
      [1] predictions/2024-25/GW1/GK.tsv exists: wc -l predictions/2024-25/GW1/GK.tsv | grep -v "^[[:space:]]*0"
      [2] Head of GK.tsv valid format: head -2 predictions/2024-25/GW1/GK.tsv | grep -c "player_id"
      [3] Investigation document written: wc -l .planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md | awk '{if ($1 >= 30) print "PASS"; else print "FAIL"}'
    </automated>
  </verify>
  <done>
    09-DATA-INVESTIGATION.md created with root cause analysis, fix recommendation (regenerate OR skip 2024-25), and effort estimate.
    If GK.tsv missing: Path to regeneration via model.py -save documented.
    If data complete but logic error: Code location identified for Phase 9 fix task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Attempt quick fix or document skip decision</name>
  <files>predictions/2024-25/, .planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md</files>
  <read_first>
    - .planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md (from Task 1 output)
    - /Users/bentindal/Desktop/coding/FPL-Auto/CLAUDE.md (model.py command syntax)
  </read_first>
  <action>
Based on Task 1 investigation:

If root cause is MISSING GK.tsv (most likely):
  - Run: python3 model.py -season 2024-25 -save
  - This regenerates predictions/2024-25/GW{1-38}/{GK,DEF,MID,FWD}.tsv
  - Estimated time: 5-10 minutes
  - Validate: Check predictions/2024-25/GW1/GK.tsv exists with > 20 lines

If root cause is INCOMPLETE CSVS or LOGIC EDGE CASE (less likely):
  - Document decision: "Proceed with 2023-24-only validation for Phase 9"
  - Rationale: "Fix requires code changes beyond data regeneration; cross-season caveat acceptable per CONTEXT.md decision #1"
  - Add caveat to 09-DATA-INVESTIGATION.md: "2024-25 validation deferred; Phase 9 uses 2023-24 + historical 2021-22, 2022-23"

Update 09-DATA-INVESTIGATION.md with final decision and timestamp.
  </action>
  <verify>
    <automated>
      [If regeneration:] Test 2024-25 season start on PHASE_8_OPTIMAL:
        python3 -c "
from manager import run_season
from fpl_auto.strategies import PHASE_8_OPTIMAL
try:
  result = run_season(PHASE_8_OPTIMAL, '2024-25')
  print('SUCCESS: 2024-25 initialization passed; total_points='+str(result.get('total_points', 'N/A')))
except Exception as e:
  if 'no GK available' in str(e):
    print('FAILED: GK error persists')
  else:
    print('FAILED: Different error: '+str(e)[:100])
"

      [If skip decision:] Verify caveat documented in 09-DATA-INVESTIGATION.md:
        grep -c "2024-25.*deferred\|skip.*2024-25\|caveat" .planning/phases/09-performance-validation/09-DATA-INVESTIGATION.md | grep -v "^0"
    </automated>
  </verify>
  <done>
    Either:
    (A) predictions/2024-25/ fully regenerated, test passes on GW1, ready for Phase 8 revalidation
    OR
    (B) Decision documented: "Phase 9 proceeds with 2023-24 walk-forward + historical aggregate, caveat in report"
    Both outcomes acceptable per user's pragmatic success criteria.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — internal data validation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | I (Information Disclosure) | 2024-25 data regeneration reads historical form | accept | Non-sensitive aggregate statistics; no PII involved |
| T-09-02 | D (Denial of Service) | model.py -save regeneration runs for 5-10 min | accept | Acceptable cost; single operator, internal system |

</threat_model>

<verification>
Completion:
- [ ] 09-DATA-INVESTIGATION.md written with root cause and fix decision
- [ ] If fix attempted: 2024-25 GK.tsv verified present and valid
- [ ] If fix skipped: Caveat documented and Phase 9 adjusted to 2023-24-only validation
- [ ] Status clear for next wave (Wave 2 Phase 8 revalidation OR skip to Wave 3 Phase 9)
</verification>

<success_criteria>
- Root cause identified (GK.tsv missing OR incomplete data OR logic error)
- Fix decision made (regenerate vs skip)
- 09-DATA-INVESTIGATION.md complete with rationale
- Next phase (Wave 2 revalidation OR Wave 3 validation) can proceed without blocking
</success_criteria>

<output>
After completion, commit to `.planning/phases/09-performance-validation/` with:
- `09-DATA-INVESTIGATION.md` (decision + rationale)
- Updated `09-PLAN.md` frontmatter if 2024-25 scope changes
</output>
