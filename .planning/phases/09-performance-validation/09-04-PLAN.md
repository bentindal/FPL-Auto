---
phase: 09-performance-validation
plan: 04
type: auto
wave: 3
depends_on: [09-02]
files_modified: [evaluation/temporal_audit_report.md, tests.py]
autonomous: true
requirements: [PV-02]
user_setup: []

must_haves:
  truths:
    - "Temporal audit passes on 2023-24 test season"
    - "No future-data violations detected in any decision point"
    - "Evidence artifact (temporal_audit_report.md) generated with sample GW traces"
  artifacts:
    - path: "evaluation/temporal_audit_report.md"
      provides: "Audit evidence: decision point verdicts + sample GW traces"
      min_lines: 50
---

<objective>
Execute temporal integrity audit on Phase 9 validation system using harness from Plan 09-02.

Purpose: Verify no lookahead bias in final system before Phase 9 top-100 comparison runs.

Output: temporal_audit_report.md with PASS/FAIL verdict for each decision point and full season.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-performance-validation/09-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run temporal audit and generate report</name>
  <files>evaluation/temporal_audit_report.md</files>
  <action>
Run: python3 -m unittest tests.TestPhase9TemporalAudit.test_temporal_integrity_full_season -v

This executes the temporal audit harness from Plan 09-02, generates evidence artifact.

If PASS: Continue to Phase 9 validation (Wave 3 Plans 05-07).
If FAIL: Debug violations, document caveats in report, mark as "FAIL with documented violations".

Ensure temporal_audit_report.md includes:
- Summary: X/5 decision points passed (expected: 5/5 for PASS verdict)
- Table: decision | gw_count | violations | verdict
- Sample traces: GW1, GW10, GW20, GW38 showing data access timeline
- Conclusion: "PASS - No lookahead bias detected" OR "FAIL - X violations in {decision_points}"
  </action>
  <verify>
    <automated>
      python3 -m unittest tests.TestPhase9TemporalAudit.test_temporal_integrity_full_season -v 2>&1 | tail -5
      
      # Verify report written:
      test -f evaluation/temporal_audit_report.md && echo "PASS" || echo "FAIL"
      
      # Check for verdict conclusion:
      grep -q "PASS\|FAIL" evaluation/temporal_audit_report.md && echo "Report valid"
    </automated>
  </verify>
  <done>
    Temporal audit executed on 2023-24.
    Evidence artifact (temporal_audit_report.md) generated.
    Verdict: PASS (no violations) or FAIL with documented caveats.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — testing only.

## STRIDE Threat Register

No threats — read-only instrumentation, no state changes.
</threat_model>

<verification>
- [ ] temporal_audit_report.md generated
- [ ] test_temporal_integrity_full_season PASSES or FAILS with clear verdict
- [ ] Report includes sample GW traces and decision point summary
</verification>

<success_criteria>
temporal_audit_report.md written with PASS verdict (no violations) OR documented caveats.
Phase 9 validation can proceed (with caveat if FAIL with documented edge cases).
</success_criteria>

<output>
Commit to `.planning/phases/09-performance-validation/` with evaluation/temporal_audit_report.md
</output>
