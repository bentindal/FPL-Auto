---
phase: 04-feature-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fpl_auto/data.py
  - fpl_auto/evaluate.py
  - tests.py
autonomous: true
requirements: [FE-01, FE-02, FE-03]
user_setup: []

must_haves:
  truths:
    - "Rolling averages (5-GW, 10-GW) compute correctly within temporal boundaries"
    - "Efficiency ratios (goals/min, assists/min) are added to feature set"
    - "Position-specific features (GK save%, DEF defensive_actions/90, MID key_passes, FWD shots_on_target) implemented"
    - "VIF analysis identifies and removes multicollinear features (VIF >= 5)"
    - "All new features preserved within TimeSeriesSplit folds (no future data leakage)"
  artifacts:
    - path: "fpl_auto/data.py"
      provides: "Feature engineering methods (rolling_averages, efficiency_ratios, position_specific_features, compute_vif)"
      exports: ["engineer_features_on_gw_data()"]
    - path: "fpl_auto/evaluate.py"
      provides: "VIF computation and reporting"
      exports: ["display_feature_vif()"]
    - path: "tests.py"
      provides: "Tests for rolling stats temporal integrity, efficiency ratio computation, VIF threshold validation"
      exports: ["TestFeatureEngineering (5 test methods)"]
    - path: ".planning/FEATURE_REGISTRY_v1.md"
      provides: "Documented list of all 35-50 features with definitions and baseline RMSE impact"
  key_links:
    - from: "fpl_auto/data.py (engineer_features_on_gw_data)"
      to: "fpl_auto/data.py (get_pos_data)"
      via: "Feature engineering applied after position filtering, before model training"
      pattern: "get_pos_data.*→.*engineer_features"
    - from: "model.py (main)"
      to: "fpl_auto/data.py (engineer_features_on_gw_data)"
      via: "avg_player_data returns engineered features"
      pattern: "engineer_features.*avg_player_data"
    - from: "tests.py (TestFeatureEngineering)"
      to: "fpl_auto/data.py (rolling_averages, efficiency_ratios)"
      via: "Temporal integrity verification using dummy data"
      pattern: "TimeSeriesSplit.*gw_data\[.*-5:\]"

---

<objective>
Implement ~15-20 engineered features (rolling stats, efficiency ratios, position-specific metrics) and validate multicollinearity via VIF analysis. Establish feature engineering workflow and create feature registry documenting all improvements.

**Purpose:** Expand feature set from ~20 raw features to ~35-40 features, capturing form momentum, efficiency patterns, and position-specific signals. VIF filtering prevents overfitting from redundant features.

**Output:** 
- Engineer_features_on_gw_data() function adding rolling 5-GW/10-GW averages, efficiency ratios, and position-specific features
- VIF analysis and feature removal (threshold: < 5)
- Feature registry mapping each new feature to expected RMSE impact
- Temporal integrity tests confirming no future-data leakage in rolling stat computation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Reference Phase 3 outputs for baseline RMSE metrics and model infrastructure validation.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feature-engineering/CONTEXT.md
@.planning/research/MODEL_IMPROVEMENT.md

# Phase 3 outputs (baseline reference)
@.planning/phases/03-model-infrastructure/03-03-SUMMARY.md

# Codebase references (feature extraction points)
@/Users/bentindal/Desktop/coding/FPL-Auto/fpl_auto/data.py (get_gw_data, get_pos_data, avg_player_data)
@/Users/bentindal/Desktop/coding/FPL-Auto/fpl_auto/predictor.py (Pipeline infrastructure)

<interfaces>
From fpl_auto/data.py (current GW data extraction):

```python
def get_gw_data(self, season, week_num):
    """Returns DataFrame with 23 columns: name, position, team, assists, bps, clean_sheets, 
    creativity, goals_conceded, goals_scored, ict_index, influence, minutes, own_goals, 
    penalties_missed, penalties_saved, red_cards, saves, threat, total_points, yellow_cards, 
    selected, was_home, value"""

def get_pos_data(self, season, week_num, position):
    """Returns position-filtered data + recent_minutes_ratio (computed from 3-GW lookback).
    Drops: position, team, ict_index columns before returning."""

def avg_player_data(self, season, from_gw, to_gw):
    """Aggregates per-player stats across GW range, averaged by player name.
    Returns 4 DataFrames (GK, DEF, MID, FWD) for training."""
```

These are the entry points where feature engineering will integrate. Rolling stats will compute 
from historical GW data (looping back from current GW); efficiency ratios will be computed 
in-place; position-specific features will be conditionally added based on position.

No modifications to public API required — feature engineering adds new columns to existing DataFrames.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Rolling Stats & Efficiency Ratios</name>
  <files>fpl_auto/data.py</files>
  <action>
Add `engineer_features_on_gw_data(gw_data, season, week_num, fpl_data_instance, position)` method to FplData class.

This function runs AFTER get_pos_data() and adds ~12-15 new features:

**Rolling Averages (window 5-GW, 10-GW) — per position:**
  - minutes_rolling_5gw, minutes_rolling_10gw
  - influence_rolling_5gw, influence_rolling_10gw
  - assists_rolling_5gw, assists_rolling_10gw
  - goals_scored_rolling_5gw, goals_scored_rolling_10gw
  - creativity_rolling_5gw, creativity_rolling_10gw
  - threat_rolling_5gw, threat_rolling_10gw

**Efficiency Ratios (always computed if minutes > 0):**
  - efficiency_goals_per_90 = (goals_scored + 1) / (minutes/90 + 1)  [add 1 to avoid div-by-zero for bench players]
  - efficiency_assists_per_90 = (assists + 1) / (minutes/90 + 1)
  - efficiency_creativity_per_min = creativity / (minutes + 1)
  - efficiency_threat_per_min = threat / (minutes + 1)

**Position-Specific Features:**
  - GK: saves_per_90 = saves / (minutes/90 + 1)
  - GK: save_percentage_safe = saves / (saves + goals_conceded + 1)  [+1 to avoid div-by-zero]
  - DEF: clean_sheets_per_90 = clean_sheets / (minutes/90 + 1)
  - DEF: defensive_actions_per_90 = (saves + 0 for DEF, estimate from team data if available)  [stub: use team clean_sheets ratio as proxy]
  - MID: key_passes_proxy = (assists * 2 + creativity * 0.5)  [proxy using existing assists/creativity; refine if FPL API adds key_passes later]
  - FWD: shots_on_target_proxy = (goals_scored * 2 + threat * 0.3)  [proxy using goals/threat ratio]

**Implementation notes:**
  - Rolling stats loop backward from current GW using fpl_data_instance.get_gw_data(season, week_num - offset)
  - Handles missing data (NaN): Fill with 0 if player didn't play in that GW (minutes==0)
  - Temporal rule: Never access future GW data. Always check week_num >= (current_window + 1)
  - For GW 1-5, rolling_10gw uses only available history (don't pad with zeros; use actual window)

**Expected output:** gw_data DataFrame with 35-40 columns total (original 23 + recent_minutes_ratio + 12-15 new features)

**Return type:** Same gw_data shape, new columns appended, no NaN in rolling stats (fill with 0 if unavailable)

**Reference:** MODEL_IMPROVEMENT.md Section 1 (Feature Engineering Patterns) and Section 4 Example 4 (Rolling Stats + Interactions)
  </action>
  <verify>
    <automated>$PY -m unittest tests.TestFeatureEngineering.test_rolling_averages_compute_correctly -v</automated>
    <automated>$PY -m unittest tests.TestFeatureEngineering.test_efficiency_ratios_no_inf_or_nan -v</automated>
    <automated>$PY -m unittest tests.TestFeatureEngineering.test_position_specific_features_present -v</automated>
  </verify>
  <done>
All rolling stats (5-GW, 10-GW) for minutes/influence/assists/goals_scored/creativity/threat computed without NaN or Inf. Efficiency ratios (goals_per_90, assists_per_90, etc) added. Position-specific features (GK save%, DEF clean_sheets_per_90, MID key_passes_proxy, FWD shots_on_target_proxy) present. Unit tests passing: at least 3 test methods in TestFeatureEngineering verify rolling stat computation and efficiency ratio bounds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement VIF Analysis & Feature Filtering</name>
  <files>fpl_auto/evaluate.py</files>
  <action>
Add `display_feature_vif(X_matrix, feature_names, position, threshold=5.0)` function to evaluate.py.

This function computes Variance Inflation Factor (VIF) for each feature and reports high-multicollinearity features.

**Implementation:**
  - Use statsmodels.stats.outliers_influence.variance_inflation_factor
  - Compute VIF for all features in X_matrix (numpy array or DataFrame)
  - Return DataFrame with columns: feature_name, vif_value
  - Print summary: features with VIF >= threshold, ranked by VIF descending
  - Return list of feature_names to DROP (VIF >= threshold)

**Output format:**
```
===== VIF Analysis for DEF (GW 15) =====
Feature                    VIF
------------------------------------
influence_rolling_5gw      8.2   <- DROP (VIF >= 5)
influence_rolling_10gw     7.9   <- DROP (VIF >= 5)
assists_rolling_5gw        4.8   KEEP
efficiency_assists_per_90  3.1   KEEP
clean_sheets_per_90        2.5   KEEP
...
Recommended action: Drop 2 features (influence_rolling_5gw, influence_rolling_10gw)
```

**Integration with feature engineering:**
  - Called AFTER engineer_features_on_gw_data() generates features
  - Called BEFORE feeding X matrix to model.fit()
  - Features to DROP are filtered out from X before training
  - Dropped features logged to .planning/FEATURE_REGISTRY_v1.md with reason (VIF >= 5)

**Reference:** MODEL_IMPROVEMENT.md Section 4, Pitfall 5 (Adding Features Without Removing Collinear Ones)
  </action>
  <verify>
    <automated>$PY -m unittest tests.TestFeatureEngineering.test_vif_computation_returns_valid_scores -v</automated>
    <automated>$PY -m unittest tests.TestFeatureEngineering.test_vif_threshold_filtering -v</automated>
  </verify>
  <done>
display_feature_vif() computes VIF for all features, returns features with VIF >= 5.0, formats output with feature names and VIF values. Unit tests passing: VIF scores are numeric, non-negative, and threshold filtering correctly identifies high-multicollinearity features. Dropped features logged for documentation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Temporal Integrity Tests & Feature Registry</name>
  <files>tests.py</files>
  <action>
Add TestFeatureEngineering class with 5 test methods to tests.py to verify rolling stats temporal integrity and feature bounds:

**Test 1: test_rolling_averages_compute_correctly**
  - Create dummy gw_data for 5 consecutive gameweeks (GW1-GW5) with known player data
  - Call engineer_features_on_gw_data() for GW5
  - Verify rolling_5gw averages match hand-calculated values (e.g., minutes_rolling_5gw = avg of GW1-GW5 minutes)
  - Assert: no NaN in rolling stat columns, rolling_5gw values within reasonable bounds (0-100 for minutes, 0-10 for influence)

**Test 2: test_rolling_stats_respect_temporal_boundary**
  - Create mock gw_data spanning GW1-GW10
  - Call engineer_features_on_gw_data() for GW8, verify rolling_10gw uses only GW1-GW8 (NOT GW9-GW10)
  - Use temporal gate check: assert max(rolling_10gw) <= max(actual_data_in_GW1_to_GW8)
  - Confirm: no future-data leakage into rolling window

**Test 3: test_efficiency_ratios_no_inf_or_nan**
  - Create gw_data with edge cases: minutes=0 (bench player), goals_scored=0 (defender)
  - Call engineer_features_on_gw_data()
  - Assert: efficiency_goals_per_90, efficiency_assists_per_90 are finite (no Inf), no NaN values
  - Assert: efficiency ratios clipped to reasonable bounds (e.g., 0-100 for goals_per_90)

**Test 4: test_position_specific_features_present**
  - For position='GK': verify saves_per_90 and save_percentage_safe columns exist and are numeric
  - For position='DEF': verify clean_sheets_per_90 column exists
  - For position='MID': verify key_passes_proxy column exists
  - For position='FWD': verify shots_on_target_proxy column exists
  - Assert: no missing columns, all values finite

**Test 5: test_vif_threshold_filtering**
  - Create X matrix with 10 intentionally correlated features (e.g., feature1, feature1*2, feature1*3)
  - Call display_feature_vif(X, feature_names, position='test', threshold=5.0)
  - Assert: returned drop_list contains the redundant features (VIF >= 5)
  - Assert: returned keep_list excludes them
  - Verify output format is readable (DataFrame or list with feature names)

**Register tests in unittest suite:**
  - Class: TestFeatureEngineering(unittest.TestCase)
  - SetUp: Create minimal FplData instance or mock (use 2021-22 season, existing data)
  - Teardown: None (stateless)

**Reference:** CONTEXT.md Technical Constraints section; MODEL_IMPROVEMENT.md Section 3 Iteration Workflow (Example 4 shows rolling stats implementation)
  </action>
  <verify>
    <automated>$PY -m unittest tests.TestFeatureEngineering -v</automated>
  </verify>
  <done>
All 5 TestFeatureEngineering tests passing. Rolling averages compute correctly within temporal boundaries (GW5 rolling_5gw uses only GW1-GW5 data, not future GWs). Efficiency ratios are finite and bounded. Position-specific features present for all 4 positions. VIF threshold filtering identifies high-multicollinearity features. Test output shows all assertions pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Feature Registry & Document Baseline Impact</name>
  <files>.planning/FEATURE_REGISTRY_v1.md</files>
  <action>
Create comprehensive feature registry documenting all ~35-40 features with baseline RMSE impact estimates.

**File:** `.planning/FEATURE_REGISTRY_v1.md`

**Structure:**

```markdown
# Feature Registry — Phase 4 v1.0

**Date:** [Today's date]
**Total Features:** ~35-40 (raw: ~23, engineered: ~12-17)
**VIF Threshold:** 5.0
**Target Baseline:** 0.3718 (Phase 3) → Target: ≤ 0.3532 (≥5% improvement)

---

## Raw Features (~23)

| Feature | Type | Source | Description | Baseline RMSE Impact |
|---------|------|--------|-------------|---------------------|
| assists | count | get_gw_data() | Assists in current GW | Moderate (MID/FWD priority) |
| bps | score | get_gw_data() | Bonus points awarded | Low (correlated with points) |
| clean_sheets | count | get_gw_data() | Clean sheets in current GW | High (DEF/GK priority) |
| creativity | float | get_gw_data() | FPL creativity metric | Moderate (MID/FWD) |
| goals_conceded | count | get_gw_data() | Goals conceded (team stat) | Moderate (DEF/GK) |
| goals_scored | count | get_gw_data() | Goals scored in GW | High (FWD priority) |
| influence | float | get_gw_data() | FPL influence metric | High (all positions) |
| minutes | int | get_gw_data() | Minutes played in GW | High (all positions) |
| own_goals | count | get_gw_data() | Own goals (rare) | Low |
| penalties_missed | count | get_gw_data() | Penalties missed | Very Low |
| penalties_saved | count | get_gw_data() | Penalties saved (GK) | Very Low |
| red_cards | count | get_gw_data() | Red cards | Very Low |
| saves | count | get_gw_data() | Saves (GK) | High (GK priority) |
| threat | float | get_gw_data() | FPL threat metric | High (FWD priority) |
| total_points | int | get_gw_data() | Points awarded in GW | High (label proxy) |
| yellow_cards | count | get_gw_data() | Yellow cards | Low |
| selected | float | get_gw_data() | Ownership % | Low-Moderate (differential signal) |
| was_home | bool | get_gw_data() | Home vs Away | Low-Moderate |
| value | float | get_gw_data() | Player price | Moderate (budget constraint signal) |
| strength_attack_home/away | float | get_team_list() | Team attack strength rating | Moderate (fixture context) |
| strength_defence_home/away | float | get_team_list() | Team defence strength rating | Moderate (fixture context) |
| recent_minutes_ratio | float | get_pos_data() | 3-GW lookback minutes ratio | Moderate (form signal) |

---

## Engineered Features (~12-17)

### Rolling Averages (5-GW window)

| Feature | Type | Position(s) | Expected Impact | VIF Risk | Status |
|---------|------|-------------|-----------------|----------|--------|
| minutes_rolling_5gw | float | ALL | High (form baseline) | HIGH (corr. with minutes) | Conditional: Keep if VIF < 5; Drop if VIF >= 5 |
| influence_rolling_5gw | float | ALL | High (form signal) | **HIGH (corr. with influence)** | ⚠️ LIKELY DROP due to VIF |
| assists_rolling_5gw | float | MID/FWD | High (goal-scoring form) | MEDIUM | Keep if VIF < 5 |
| goals_scored_rolling_5gw | float | FWD | Very High (FWD priority) | MEDIUM | Keep if VIF < 5 |
| creativity_rolling_5gw | float | MID/FWD | High (passing form) | MEDIUM | Keep if VIF < 5 |
| threat_rolling_5gw | float | FWD | Very High (FWD priority) | MEDIUM | Keep if VIF < 5 |

### Rolling Averages (10-GW window)

| Feature | Type | Position(s) | Expected Impact | VIF Risk | Status |
|---------|------|-------------|-----------------|----------|--------|
| influence_rolling_10gw | float | ALL | Moderate (seasonal trend) | **HIGH (corr. with influence)** | ⚠️ LIKELY DROP due to VIF |
| assists_rolling_10gw | float | MID/FWD | Moderate | MEDIUM | Keep if VIF < 5 |
| creativity_rolling_10gw | float | MID | Moderate | MEDIUM | Keep if VIF < 5 |
| threat_rolling_10gw | float | FWD | Moderate | MEDIUM | Keep if VIF < 5 |

### Efficiency Ratios

| Feature | Type | Position(s) | Expected Impact | Formula | Status |
|---------|------|-------------|-----------------|---------|--------|
| efficiency_goals_per_90 | float | FWD | High (FWD priority) | (goals_scored + 1) / (minutes/90 + 1) | KEEP |
| efficiency_assists_per_90 | float | MID/FWD | Moderate-High | (assists + 1) / (minutes/90 + 1) | KEEP |
| efficiency_creativity_per_min | float | MID | Moderate | creativity / (minutes + 1) | KEEP |
| efficiency_threat_per_min | float | FWD | Moderate-High | threat / (minutes + 1) | KEEP |

### Position-Specific Features

| Feature | Position | Expected Impact | Formula | Status |
|---------|----------|-----------------|---------|--------|
| saves_per_90 | GK | High (GK priority) | saves / (minutes/90 + 1) | KEEP |
| save_percentage_safe | GK | High (efficiency) | saves / (saves + goals_conceded + 1) | KEEP |
| clean_sheets_per_90 | DEF | High (DEF priority) | clean_sheets / (minutes/90 + 1) | KEEP |
| defensive_actions_per_90 | DEF | Moderate (proxy) | (saves + 0) / (minutes/90 + 1)  [stub] | CONDITIONAL |
| key_passes_proxy | MID | Moderate (proxy) | (assists * 2 + creativity * 0.5) | CONDITIONAL |
| shots_on_target_proxy | FWD | High (proxy) | (goals_scored * 2 + threat * 0.3) | CONDITIONAL |

---

## Feature Selection Strategy

### Initial Recommendation (before VIF filtering):
- **KEEP:** All raw features (baseline foundation)
- **KEEP:** All efficiency ratios (low VIF risk, high interpretability)
- **KEEP:** Position-specific features (conditional on position, low overlap)
- **EVALUATE:** Rolling_5gw and rolling_10gw (VIF filtering will decide)
- **LIKELY DROP:** influence_rolling_5gw, influence_rolling_10gw (high multicollinearity with raw influence)

### Post-VIF Filtering (actual decision):
Run VIF analysis after engineer_features_on_gw_data() generates features. Update this registry with actual VIF scores and drop decisions per position.

---

## Expected RMSE Impact

### Hypothesis by Feature Category:

| Category | Estimated RMSE Reduction | Reasoning |
|----------|--------------------------|-----------|
| Efficiency Ratios | -2% to -4% | Normalize for playing time (bench players won't noise model) |
| Rolling 5-GW Averages | -1% to -3% | Capture current form (more stable than single-GW) |
| Rolling 10-GW Averages | -0.5% to -1.5% | Seasonal trend, less immediate than 5-GW |
| Position-Specific Features | -1% to -2.5% | Tailor predictions to role (saves_per_90 for GK, etc) |
| **Total Expected** | **-4.5% to -11%** | Cumulative; actual depends on VIF filtering and overlap |

**Phase 4 Target:** ≥ 5% reduction → Final RMSE ≤ 0.3532

---

## Baseline Metrics (Phase 3)

From PHASE_3_VERIFICATION.md:

| Position | Baseline RMSE | Test Set | Source |
|----------|---------------|----------|--------|
| GK | 0.33-0.39 | TimeSeriesSplit fold 5 | BASELINE_METRICS.json |
| DEF | 0.35-0.40 | TimeSeriesSplit fold 5 | BASELINE_METRICS.json |
| MID | 0.32-0.35 | TimeSeriesSplit fold 5 | BASELINE_METRICS.json |
| FWD | 0.38-0.46 | TimeSeriesSplit fold 5 | BASELINE_METRICS.json |
| **Average** | **0.3718** | — | BASELINE_METRICS.json (3-season avg) |

---

## Iteration Tracking

### Iteration 1 (Plan 04-01 & 04-02)

**Date:** [Plan completion date]
**Features Added:** ~12-17 engineered features
**VIF Filtering:** [To be filled after VIF analysis]
**Dropped Due to VIF:** [e.g., influence_rolling_5gw, influence_rolling_10gw]
**Final Feature Count:** [35-45, depending on drops]
**RMSE Change:** [To be measured in Plan 04-02]
**Status:** Pending retrain with TimeSeriesSplit

---

## Next Steps (Phase 4 Plan 02)

1. **Integrate feature engineering into model.py pipeline:**
   - Call engineer_features_on_gw_data() within avg_player_data() flow
   - Apply VIF filtering before passing X to model.fit()

2. **Retrain models with engineered features:**
   - Run model.py with `-season 2021-22 -repeat 19 -score_train_vs_test`
   - Measure RMSE change vs. baseline
   - Record per-position improvements

3. **Iterate:**
   - If RMSE improves ≥2%: keep all features, proceed to Plan 04-03 (advanced features)
   - If RMSE improves <2%: analyze which features underperform, consider alternative engineering

---

## Appendix: Implementation References

**MODEL_IMPROVEMENT.md:**
- Section 1: Feature Engineering Patterns (rolling averages, interactions, efficiency ratios)
- Section 4 Example 4: Code template for rolling stats + position-specific features
- Pitfall 5: Multicollinearity removal (VIF threshold)

**CONTEXT.md:**
- High-Impact Feature Categories (form, efficiency, position-specific, team context)
- Technical Constraints (temporal integrity, no future data, VIF < 5)

**CLAUDE.md:**
- get_gw_data() columns (baseline 23 features)
- discount_next_n_gws() pattern (multi-GW averaging already in use)
```

**Integration note:** This registry will be updated after VIF analysis (Task 2) and after retraining (Plan 04-02) with actual RMSE measurements. It serves as the living documentation of the feature engineering process.
  </action>
  <verify>
    <automated>test -f .planning/FEATURE_REGISTRY_v1.md && grep -c "^|" .planning/FEATURE_REGISTRY_v1.md | awk '{if ($1 >= 40) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>
.planning/FEATURE_REGISTRY_v1.md created with ~35-40 features documented. Registry includes: raw features (23), engineered features (~12-17), expected RMSE impact estimates, VIF risk assessment, and baseline metrics from Phase 3. Tables formatted for clarity. Feature selection strategy documented (KEEP/DROP/CONDITIONAL recommendations). Registry structured for iteration — ready to update with actual VIF scores and RMSE measurements in Plan 04-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model Input Layer | Untrusted features from engineered stats may contain NaN/Inf if temporal logic fails |
| Rolling Window | Risk: future GW data leaks into rolling averages if temporal boundary not enforced |
| Efficiency Division | Risk: division by zero in efficiency_goals_per_90 if minutes always 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | engineer_features_on_gw_data | mitigate | Validate rolling window: assert week_num - 5 >= 1 before accessing past GWs; unit test confirms no future data |
| T-04-02 | Information Disclosure | rolling_stat computation | mitigate | Temporal integrity verified via test_rolling_stats_respect_temporal_boundary: mock gw_data GW1-GW10, confirm rolling_10gw uses only GW1-GW8 at GW8 |
| T-04-03 | Denial of Service | VIF computation | mitigate | Compute VIF only on training fold (inside TimeSeriesSplit loop in model.py Plan 02), never on full dataset; VIF threshold (5.0) prevents overfitting explosion |
| T-04-04 | Elevation of Privilege | Efficiency ratio division | mitigate | Add 1 to denominators (minutes/90 + 1, minutes + 1) to prevent Inf; unit test test_efficiency_ratios_no_inf_or_nan verifies all ratios finite |
| T-04-05 | Repudiation | Feature registry drift | accept | Feature registry is documentation; implementation is source of truth; drift unlikely as code drives registry updates |

All mitigations specific to implementation (no generic "validate inputs" entries).
</threat_model>

<verification>
**Unit test execution:**
```bash
PY=/Library/Frameworks/Python.framework/Versions/3.10/bin/python3
$PY -m unittest tests.TestFeatureEngineering -v
```

**Expected output:** 5/5 tests passing
- test_rolling_averages_compute_correctly ✅
- test_rolling_stats_respect_temporal_boundary ✅
- test_efficiency_ratios_no_inf_or_nan ✅
- test_position_specific_features_present ✅
- test_vif_threshold_filtering ✅

**File integrity check:**
```bash
# Verify fpl_auto/data.py has engineer_features_on_gw_data function
grep -c "def engineer_features_on_gw_data" fpl_auto/data.py
# Expected: 1

# Verify fpl_auto/evaluate.py has display_feature_vif function
grep -c "def display_feature_vif" fpl_auto/evaluate.py
# Expected: 1

# Verify feature registry exists
test -f .planning/FEATURE_REGISTRY_v1.md && echo "PASS" || echo "FAIL"
```

**Integration check:**
Import and run minimal example:
```bash
$PY -c "from fpl_auto.data import FplData, get_fpl_data; from fpl_auto.evaluate import display_feature_vif; print('Imports successful')"
```
</verification>

<success_criteria>
**Plan 04-01 Complete when:**

1. ✅ engineer_features_on_gw_data() implemented in fpl_auto/data.py
   - Rolling 5-GW and 10-GW averages for: minutes, influence, assists, goals_scored, creativity, threat
   - Efficiency ratios: goals_per_90, assists_per_90, creativity_per_min, threat_per_min
   - Position-specific: GK (saves_per_90, save_percentage), DEF (clean_sheets_per_90), MID (key_passes_proxy), FWD (shots_on_target_proxy)

2. ✅ display_feature_vif() implemented in fpl_auto/evaluate.py
   - Computes VIF for all features
   - Returns DataFrame with feature_name, vif_value columns
   - Identifies features with VIF >= 5.0 (multicollinearity threshold)
   - Formats output for human readability

3. ✅ TestFeatureEngineering class added to tests.py with 5 test methods
   - test_rolling_averages_compute_correctly
   - test_rolling_stats_respect_temporal_boundary (no future data leakage)
   - test_efficiency_ratios_no_inf_or_nan
   - test_position_specific_features_present
   - test_vif_threshold_filtering
   - All tests passing (5/5)

4. ✅ Feature registry created at .planning/FEATURE_REGISTRY_v1.md
   - Documents ~35-40 total features (raw + engineered)
   - Baseline metrics from Phase 3 (0.3718 RMSE)
   - RMSE impact estimates per feature category
   - VIF risk assessment
   - Ready for iteration updates in Plan 04-02

5. ✅ No regressions in existing code
   - manager.py still works (no changes needed yet)
   - model.py still runs (feature integration deferred to Plan 04-02)
   - All Phase 3 tests still passing

6. ✅ Temporal integrity verified
   - Unit tests confirm rolling stats use only past data (no future GW access)
   - All new features respect TimeSeriesSplit fold boundaries

---

**Ready for Plan 04-02 when above criteria met.**

Plan 04-02 will integrate feature engineering into the model training pipeline and measure actual RMSE improvements.
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-engineering/04-01-SUMMARY.md`
</output>
