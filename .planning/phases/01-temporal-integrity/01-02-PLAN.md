---
phase: 01-temporal-integrity
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tests.py
  - fpl_auto/temporal.py
  - .planning/phases/01-temporal-integrity/TEMPORAL_INTEGRITY_ARCHITECTURE.md
autonomous: true
requirements: [TI-03, TI-04]
user_setup: []

must_haves:
  truths:
    - "Automated tests detect when model training includes current-GW data"
    - "Automated tests verify TemporalGate blocks future data access"
    - "Automated tests confirm pre-trained predictions exist before season run"
    - "Audit trail proves no unintended future data was accessed during decisions"
  artifacts:
    - path: "tests.py"
      provides: "Test suite for temporal violations"
      contains: "4+ test methods for temporal enforcement"
    - path: "fpl_auto/temporal.py"
      provides: "TemporalGate class with audit trail support"
      exports: ["TemporalGate.audit_trail()"]
    - path: ".planning/phases/01-temporal-integrity/TEMPORAL_INTEGRITY_ARCHITECTURE.md"
      provides: "Documentation of temporal data availability per gameweek"
      min_lines: 60
  key_links:
    - from: "tests.py"
      to: "fpl_auto/temporal.py.TemporalGate"
      via: "Test imports and instantiates TemporalGate; verifies safe_read_* methods"
      pattern: "from fpl_auto.temporal import TemporalGate"
    - from: "tests.py"
      to: "model.py"
      via: "Test checks that model training uses corrected boundaries (i-20 to i-1)"
      pattern: "get_training_data_all.*i - training_prev_weeks - 1"
    - from: "tests.py"
      to: "fpl_auto/data.py"
      via: "Test verifies temporal semantics of actual_points_dict and get_gw_data"
      pattern: "fpl.actual_points_dict|fpl.get_gw_data"
---

<objective>
Create comprehensive automated test suite for temporal integrity enforcement and document the temporal data architecture.

Purpose: Detect temporal violations early (during CI/testing), prevent future lookahead bias bugs, establish verifiable contracts for data access patterns.

Output:
- 4 automated tests in tests.py for temporal violations (TI-04)
- Temporal Integrity Architecture document describing data availability per gameweek (TI-03)
- TemporalGate enhanced with detailed audit trail for debugging
- Clear verification that model training boundaries are correct
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bentindal/Desktop/coding/FPL-Auto/.planning/research/TEMPORAL_INTEGRITY.md
@/Users/bentindal/Desktop/coding/FPL-Auto/fpl_auto/temporal.py
@/Users/bentindal/Desktop/coding/FPL-Auto/tests.py
@/Users/bentindal/Desktop/coding/FPL-Auto/model.py
@/Users/bentindal/Desktop/coding/FPL-Auto/fpl_auto/data.py
</context>

<interfaces>
From fpl_auto/temporal.py (created in Plan 01):
```python
class TemporalGate:
    def __init__(self, season: str, decision_gameweek: int)
    def safe_read_historical_form(self, target_gw: int) -> bool
    def safe_read_predictions(self, target_gw: int) -> bool
    def safe_read_fixture_metadata(self) -> bool
    def audit_trail(self) -> list  # Returns [(data_type, accessed_gw, decision_gw, allowed), ...]
```

From tests.py (existing test suite):
```python
class TestTransferInAllowed(unittest.TestCase):
    # ... existing tests ...

# NEW: Add test class TestTemporalIntegrity below existing tests
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add temporal violation detection tests to tests.py (TI-04)</name>
  <files>tests.py</files>
  <action>
Add a new test class TestTemporalIntegrity to tests.py with four test methods. Insert after the existing test classes (after TestAddPlayer, before any imports).

Each test validates a specific temporal contract. Write tests as simple unit tests that verify the TemporalGate class and data access patterns work correctly.

Test 1: test_temporal_gate_blocks_future_historical_data()
- Instantiate TemporalGate('2023-24', decision_gameweek=10)
- Call safe_read_historical_form(target_gw=11) — should raise TemporalViolationError
- Call safe_read_historical_form(target_gw=9) — should return True
- Assert error message contains "GW11" and "GW10"

Test 2: test_temporal_gate_only_allows_current_predictions()
- Instantiate TemporalGate('2023-24', decision_gameweek=10)
- Call safe_read_predictions(target_gw=10) — should return True
- Call safe_read_predictions(target_gw=11) — should raise TemporalViolationError
- Call safe_read_predictions(target_gw=9) — should raise TemporalViolationError
- Assert error indicates only GW10 allowed

Test 3: test_temporal_gate_fixtures_always_safe()
- Instantiate TemporalGate('2023-24', decision_gameweek=5)
- Call safe_read_fixture_metadata() — should return True
- Verify no error raised (fixtures known pre-season)

Test 4: test_audit_trail_logs_all_accesses()
- Instantiate TemporalGate('2023-24', decision_gameweek=15)
- Call safe_read_historical_form(14) — succeeds
- Call safe_read_predictions(15) — succeeds
- Call safe_read_historical_form(16) — fails
- Call audit_trail() — returns list with 3 entries
- Verify entries have format: (data_type, accessed_gw, decision_gw, allowed: bool)
- Assert third entry has allowed=False

Code style: Match existing tests.py patterns (use unittest.TestCase, self.assert*, no pytest syntax).

Add import at top of tests.py if needed:
```python
from fpl_auto.temporal import TemporalGate, TemporalViolationError
```

Location: Add new TestTemporalIntegrity class after the final existing test (after TestAddPlayer or latest test class).
  </action>
  <verify>
    <automated>python3 -m unittest tests.TestTemporalIntegrity -v 2>&1 | grep -c "OK\|test_temporal_gate"</automated>
  </verify>
  <done>
TestTemporalIntegrity class added to tests.py with 4 test methods. All tests pass and verify TemporalGate functionality.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Temporal Integrity Architecture document (TI-03 deliverable)</name>
  <files>.planning/phases/01-temporal-integrity/TEMPORAL_INTEGRITY_ARCHITECTURE.md</files>
  <action>
Create a new file .planning/phases/01-temporal-integrity/TEMPORAL_INTEGRITY_ARCHITECTURE.md documenting what data is available at each gameweek decision point.

The document should include:

1. Executive Summary (2-3 sentences)
   - State the goal: enforce temporal boundaries so strategies can only see data available at decision time
   - Reference the research findings: TEMPORAL_INTEGRITY.md

2. Data Availability Timeline (table format)
   - Column 1: Data Point (Fixture list, Historical form, Current price, Injury news, etc)
   - Column 2: Release Time (Pre-season, After match, 2:30am UTC, etc)
   - Column 3: Available During GW(N) Decision? (Yes/No)
   - Column 4: Reference (from RESEARCH.md)
   Include 6-8 key data points

3. Safe Access Contracts (per decision phase)
   - "Before GW(N) Deadline (Decision Phase)"
     - Safe to read: List 5-6 items with ✅
     - NOT safe to read: List 4-5 items with ❌
   - "After GW(N) Matches Complete (Scoring Phase)"
     - Safe to read: List 3-4 items with ✅
     - NOT safe to read: List 2-3 items with ❌

4. Implementation: TemporalGate Class
   - Brief description of safe_read_historical_form, safe_read_predictions, safe_read_fixture_metadata
   - Show which method is used for which data type
   - Include code example: How to use TemporalGate in Team.__init__ (not implemented yet, but documented for Phase 2)

5. Testing Strategy (TI-04)
   - List the 4 automated tests added in Task 1
   - Explain what each test verifies
   - Show example audit trail output

6. Critical Boundaries Enforced
   - Model training: GW(i-20) to GW(i-1), never GW(i) itself
   - Historical form: Only GW(1) to GW(N-1) during GW(N) decisions
   - Predictions: Only GW(N) for GW(N) decisions
   - Fixture data: Full season available (known pre-season)

7. Violations & Recovery
   - What happens when a violation is detected (TemporalViolationError raised)
   - How to read audit_trail() output
   - Common mistakes to avoid

Content should be clear enough for future developers to understand what data is available when, and why each boundary matters. Reference specific line numbers in code and test names where relevant.

Minimum 60 lines, well-formatted markdown.
  </action>
  <verify>
    <automated>wc -l /Users/bentindal/Desktop/coding/FPL-Auto/.planning/phases/01-temporal-integrity/TEMPORAL_INTEGRITY_ARCHITECTURE.md | grep -E "[6-9][0-9]|[0-9]{3}" && grep -c "TemporalGate\|Safe\|NOT safe" /Users/bentindal/Desktop/coding/FPL-Auto/.planning/phases/01-temporal-integrity/TEMPORAL_INTEGRITY_ARCHITECTURE.md</automated>
  </verify>
  <done>
TEMPORAL_INTEGRITY_ARCHITECTURE.md created with 60+ lines documenting:
- Data availability timeline per gameweek
- Safe access contracts (decision phase vs scoring phase)
- TemporalGate class usage and safe_read_* methods
- Testing strategy and audit trail explanation
- Critical boundaries (model training, historical form, predictions, fixtures)
- Violation handling and common mistakes
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify existing test suite still passes (no regressions)</name>
  <files>tests.py</files>
  <action>
Run the full test suite to ensure no regressions from changes in Plan 01 and Plan 02. This validates that:
1. TemporalGate implementation doesn't break existing Team logic
2. model.py correction (line 58) doesn't break the model training flow
3. FplData docstrings don't affect runtime behavior

Run command:
```bash
python3 -m unittest tests -v
```

Expected output:
- All existing test classes pass (TestTransferInAllowed, TestAddPlayer, etc)
- New TestTemporalIntegrity class passes all 4 tests
- No failures or errors
- Output should show "OK" at end

If any test fails, investigate and fix before proceeding. Common issues:
- TemporalGate import missing — add to tests.py
- TemporalViolationError not raised correctly — check temporal.py implementation
- Docstrings caused syntax errors — verify fpl_auto/data.py formatting

This task validates the integration of both Phase 1 plans.
  </action>
  <verify>
    <automated>python3 -m unittest tests -v 2>&1 | tail -1 | grep -q "OK" && echo "All tests passed"</automated>
  </verify>
  <done>
Full test suite passes with all existing and new tests green. No regressions detected.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → TemporalGate | Tests verify TemporalGate correctly rejects invalid access |
| Code review → Violations | Automated tests catch temporal violations before code merged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Undetected future data access | mitigate | Automated tests in test_audit_trail_logs_all_accesses() detect all accesses (TI-04) |
| T-02-02 | Tampering | Tests bypass TemporalGate | mitigate | Tests instantiate TemporalGate directly; no mock workarounds allowed |
| T-02-03 | Repudiation | Violations not logged | mitigate | audit_trail() method provides detailed access log (TI-04) |

</threat_model>

<verification>
Phase 1 testing plan verified when:
1. TestTemporalIntegrity class created with 4 test methods
2. All 4 temporal tests pass
3. TEMPORAL_INTEGRITY_ARCHITECTURE.md created with 60+ lines
4. Existing test suite still passes (no regressions)
5. audit_trail() method works correctly
</verification>

<success_criteria>
1. TestTemporalIntegrity class exists with test_temporal_gate_blocks_future_historical_data, test_temporal_gate_only_allows_current_predictions, test_temporal_gate_fixtures_always_safe, test_audit_trail_logs_all_accesses
2. All 4 tests pass and verify TemporalGate correctness
3. TEMPORAL_INTEGRITY_ARCHITECTURE.md documents data availability per gameweek
4. Full test suite (old + new) passes with no regressions
5. Code review can reference tests as automated violation detectors
6. audit_trail() output is clear and actionable for debugging
</success_criteria>

<output>
After completion, create `.planning/phases/01-temporal-integrity/01-02-SUMMARY.md` documenting:
- TestTemporalIntegrity test methods implemented and passing
- TEMPORAL_INTEGRITY_ARCHITECTURE.md structure and content
- Test coverage: what violations are detected, what can be added later
- Regression testing results
- Readiness for Phase 2 (Team integration with TemporalGate)
</output>
